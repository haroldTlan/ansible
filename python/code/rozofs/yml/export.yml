---
- hosts: "{{ export }}"
  remote_user: root
  gather_facts: false
  vars_files:
    - vars/rozofs.yml  
  tasks:
    - name: exportInit
      shell: rozo volume expand {{ expand }} --vid {{ vid }} --layout {{ layout }}  --exportd {{ export }}
